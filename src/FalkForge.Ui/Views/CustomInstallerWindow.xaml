<Window x:Class="FalkForge.Ui.Views.CustomInstallerWindow"
        x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource WindowBackground}">
    <Window.Resources>
        <SolidColorBrush x:Key="WindowBackground" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#0078D4"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Page content area -->
        <ContentPresenter Grid.Row="0" Content="{Binding CurrentView}"/>

        <!-- Navigation bar -->
        <Border Grid.Row="1"
                Background="{DynamicResource WindowBackground}"
                BorderThickness="0,1,0,0"
                BorderBrush="#E0E0E0"
                Padding="16,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Content="Back"
                        Command="{Binding BackCommand}"
                        IsEnabled="{Binding CanGoBack}"
                        MinWidth="75" Margin="0,0,8,0"/>

                <Grid Grid.Column="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <ContentControl Grid.Column="0"
                                    Content="{Binding LanguageSelector}"
                                    VerticalAlignment="Center"
                                    Margin="8,0,0,0">
                        <ContentControl.Style>
                            <Style TargetType="ContentControl">
                                <Style.Triggers>
                                    <Trigger Property="Content" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ContentControl.Style>
                    </ContentControl>
                    <TextBlock Grid.Column="1"
                               Text="{Binding StatusMessage}"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Center"
                               Foreground="#CC0000"
                               TextWrapping="Wrap"/>
                </Grid>

                <Button Grid.Column="2" Content="Cancel"
                        Command="{Binding CancelCommand}"
                        MinWidth="75" Margin="8,0"/>

                <Button Grid.Column="3" Content="Next"
                        Command="{Binding NextCommand}"
                        IsEnabled="{Binding CanGoNext}"
                        MinWidth="75"
                        Background="{DynamicResource AccentBrush}"
                        Foreground="White"/>
            </Grid>
        </Border>

        <!-- Drag handle for borderless mode (invisible, covers top area) -->
        <Rectangle Grid.Row="0" Height="32" VerticalAlignment="Top"
                   Fill="Transparent"
                   x:Name="DragHandle"
                   MouseLeftButtonDown="DragHandle_MouseLeftButtonDown"/>
    </Grid>
</Window>
