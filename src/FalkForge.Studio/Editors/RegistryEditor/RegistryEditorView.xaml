<UserControl x:Class="FalkForge.Studio.Editors.RegistryEditor.RegistryEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:FalkForge.Studio.Editors.RegistryEditor">
    <DockPanel Margin="8">
        <TextBlock DockPanel.Dock="Top" Text="Registry" FontSize="18" FontWeight="Bold" Margin="0,0,0,8" />
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="Add Entry" Click="AddEntry_Click" Padding="8,4" Margin="0,0,8,0" />
            <Button Content="Remove" Click="Remove_Click" Padding="8,4" />
        </StackPanel>
        <DataGrid ItemsSource="{Binding Entries}"
                  SelectedItem="{Binding SelectedEntry}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  Margin="0,0,0,0">
            <DataGrid.Columns>
                <DataGridComboBoxColumn Header="Root"
                                        SelectedItemBinding="{Binding Root, UpdateSourceTrigger=PropertyChanged}"
                                        ItemsSource="{x:Static local:RegistryEditorViewModel.Roots}"
                                        Width="140" />
                <DataGridTextColumn Header="Key"
                                    Binding="{Binding Key, UpdateSourceTrigger=PropertyChanged}"
                                    Width="200" />
                <DataGridTextColumn Header="Value Name"
                                    Binding="{Binding ValueName, UpdateSourceTrigger=PropertyChanged}"
                                    Width="140" />
                <DataGridComboBoxColumn Header="Value Type"
                                        SelectedItemBinding="{Binding ValueType, UpdateSourceTrigger=PropertyChanged}"
                                        ItemsSource="{x:Static local:RegistryEditorViewModel.ValueTypes}"
                                        Width="120" />
                <DataGridTextColumn Header="Value"
                                    Binding="{Binding Value, UpdateSourceTrigger=PropertyChanged}"
                                    Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
