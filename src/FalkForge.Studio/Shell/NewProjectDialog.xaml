<Window x:Class="FalkForge.Studio.Shell.NewProjectDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="New Project" Width="500" Height="350"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <DockPanel Margin="16">
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="OK" Width="80" Padding="4" Margin="0,0,8,0" IsDefault="True" Click="OK_Click" />
            <Button Content="Cancel" Width="80" Padding="4" IsCancel="True" />
        </StackPanel>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <ListBox x:Name="TemplateList" Grid.Column="0" DisplayMemberPath="Name" Margin="0,0,8,0" />
            <StackPanel Grid.Column="1">
                <TextBlock Text="{Binding SelectedItem.Name, ElementName=TemplateList}" FontSize="16" FontWeight="Bold" Margin="0,0,0,8" />
                <TextBlock Text="{Binding SelectedItem.Description, ElementName=TemplateList}" TextWrapping="Wrap" Foreground="Gray" />
            </StackPanel>
        </Grid>
    </DockPanel>
</Window>
